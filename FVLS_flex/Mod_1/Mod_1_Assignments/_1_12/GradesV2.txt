/**
 * The purpose of the program is to figure about the overall grade of the student
 * 
 * @ Author: Tejas Upadhyay
 * @ Made 6/29/2023
*/

package FVLS_flex.Mod_1.Mod_1_Assignments._1_12;

public class GradesV2 {
    public static void main(String[] args) {
        //local variables
        int numTests = 0;      //counts number of tests
        int testGrade = 0;     //individual test grade
        int totalPoints = 0;   //total points for all tests
        double average = 0.0;  //average grade
        
        numTests++; // Adds one to the varable numTest
        testGrade = 95; // The grade the studnet got on the test
        totalPoints += testGrade; // adds the value of testGrade to the value of totalPoints
        average = (double) totalPoints / numTests; // Divdes the totalPoints with numTest to get the average of all the test
        System.out.println("Test # "+ numTests +"     Test Grade: " + testGrade + "   Total Points: " + totalPoints + " Average Score: " + average); // Prints the score they got on the test the total ponits they got on all test and the their average
        
        numTests++; // Adds one to the varable numTest
        testGrade = 73; // The grade the studnet got on the test
        totalPoints += testGrade; // adds the value of testGrade to the value of totalPoints
        average = (double) totalPoints / numTests; // Divdes the totalPoints with numTest to get the average of all the test
        System.out.println("Test # "+ numTests +"     Test Grade: " + testGrade + "   Total Points: " + totalPoints + " Average Score: " + average); // Prints the score they got on the test the total ponits they got on all test and the their average
        
        numTests++; // Adds one to the varable numTest
        testGrade = 91; // The grade the studnet got on the test
        totalPoints += testGrade;
        average = (double) totalPoints / numTests; // Divdes the totalPoints with numTest to get the average of all the test
        System.out.println("Test # "+ numTests +"     Test Grade: " + testGrade + "   Total Points: " + totalPoints + " Average Score: " + average); // Prints the score they got on the test the total ponits they got on all test and the their average
        
        numTests++; // Adds one to the varable numTest
        testGrade = 82; // The grade the studnet got on the test
        totalPoints += testGrade; // adds the value of testGrade to the value of totalPoints
        average = (double) totalPoints / numTests; // Divdes the totalPoints with numTest to get the average of all the test
        System.out.println("Test # "+ numTests +"     Test Grade: " + testGrade + "   Total Points: " + totalPoints + " Average Score: " + average); // Prints the score they got on the test the total ponits they got on all test and the their average

        // Additonal Grades:
        
        numTests++; // Adds one to the varable numTest
        testGrade = 100; // The grade the studnet got on the test
        totalPoints += testGrade; // adds the value of testGrade to the value of totalPoints
        average = (double) totalPoints / numTests; // Divdes the totalPoints with numTest to get the average of all the test
        System.out.println("Test # "+ numTests +"     Test Grade: " + testGrade + "   Total Points: " + totalPoints + " Average Score: " + average); // Prints the score they got on the test the total ponits they got on all test and the their average
        
        numTests++; // Adds one to the varable numTest
        testGrade = 56; // The grade the studnet got on the test
        totalPoints += testGrade; // adds the value of testGrade to the value of totalPoints
        average = (double) totalPoints / numTests; // Divdes the totalPoints with numTest to get the average of all the test
        System.out.println("Test # "+ numTests +"     Test Grade: " + testGrade + "   Total Points: " + totalPoints + " Average Score: " + average); // Prints the score they got on the test the total ponits they got on all test and the their average

        numTests++; // Adds one to the varable numTest
        testGrade = 94; // The grade the studnet got on the test
        totalPoints += testGrade; // adds the value of testGrade to the value of totalPoints
        average = (double) totalPoints / numTests; // Divdes the totalPoints with numTest to get the average of all the test
        System.out.println("Test # "+ numTests +"     Test Grade: " + testGrade + "   Total Points: " + totalPoints + " Average Score: " + average); // Prints the score they got on the test the total ponits they got on all test and the their average

        numTests++; // Adds one to the varable numTest
        testGrade = 85; // The grade the studnet got on the test
        totalPoints += testGrade; // adds the value of testGrade to the value of totalPoints
        average = (double) totalPoints / numTests; // Divdes the totalPoints with numTest to get the average of all the test
        System.out.println("Test # "+ numTests +"     Test Grade: " + testGrade + "   Total Points: " + totalPoints + " Average Score: " + average); // Prints the score they got on the test the total ponits they got on all test and the their average
        
        numTests++; // Adds one to the varable numTest
        testGrade = 78; // The grade the studnet got on the test
        totalPoints += testGrade; // adds the value of testGrade to the value of totalPoints
        average = (double) totalPoints / numTests; // Divdes the totalPoints with numTest to get the average of all the test
        System.out.println("Test # "+ numTests +"     Test Grade: " + testGrade + "   Total Points: " + totalPoints + " Average Score: " + average); // Prints the score they got on the test the total ponits they got on all test and the their average
    }
}